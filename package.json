{
  "name": "elsinore-push-relay",
  "version": "0.1.0",
  "private": true,
  "description": "The one shared APNs relay for Elsinore sidecars: signs the provider JWT and forwards content-free push requests to Apple.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260730.1",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0",
    "wrangler": "^4.0.0",
    "@types/node": "^22.0.0"
  }
}